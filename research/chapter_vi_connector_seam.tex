\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,booktabs,longtable,array,pdflscape}
\usepackage[hidelinks]{hyperref}

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newcommand{\R}{\mathbb R}
\newcommand{\re}{\operatorname{Re}}
\newcommand{\impart}{\operatorname{Im}}
\newcommand{\ii}{\mathrm i}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

\title{A scale-free proof of the connector seam sign at Poincar\'e's point \(D\)}
\author{}
\date{August 2026}

\begin{document}
\maketitle

\begin{abstract}
The last unresolved numerical issue in the current connector construction is caused by a
double zero at the collision point \(D\). Direct interval evaluation loses the small common
Morse scale and produces boxes containing zero. We retain that scale algebraically. The
oriented real derivative is written exactly as
\[
  L\,\re X_s(L,\delta)+\delta^2\,\re Z_s(L,\delta).
\]
Here \(s=-1\) and \(s=+1\) denote the initial and final connectors. An independent
outward-rounded Python interval calculation proves
\(\re X_s\geq0.00419\) and \(\re Z_s\geq0.06558\) on both complete collars.
Since \(L>0\), the required derivative orientation follows uniformly, however small the
nonconstructively selected Morse length is. Ordinary one-variable calculus then supplies
the missing connector seam sign.
\end{abstract}

\section{Scope}

This note addresses the local-to-global seam problem for the two affine connectors at the
specific collision point \(D\). It uses analytic facts already established in the project:
the inverse-Morse endpoint can be chosen with the normalized bounds in
\S\ref{sec:boxes}, the companion collision factor is positive on the collars, and the first
factor has the required imaginary monotonicity. The new content is the real derivative sign
for the first factor.

The subsequent Lean development reuses the same bulk rows uniformly in the critical-value
coordinate and joins them to this scale-sensitive collar argument on every positive fiber.  It
therefore proves connector-radicand nonvanishing and constructs a continuous square-root sheet
on each half-open rectangle $[0,1)\times[0,1]$.  The later file
\path{ChapterVIDJointLiftedContour.lean} assembles those sheets with the outer and middle sheets
into one closed, basepoint-normalized lifted five-piece family near $D$.  A subsequent 44-shard
certificate proves nonvanishing on the complete moving angular circle in the transformed root
coordinate through the exact cutoff $2717/2744$, and constructs its principal lift.  This still
does not lift the fixed-parameter coordinate homotopy back to the original source-circle sheet,
construct the final $27/2744$ of the deformation into the terminal five-piece cycle, or derive every
source-specific multiplier, interchange, and coefficient-identification statement in \S102 from
the full Chapter~V uniform-integral problem.

\section{Map to Poincar\'e's printed proof}\label{sec:source-map}

The following table distinguishes three different situations: an actual algebraic or typographical
error in the printed formulas; a substantial argument that Poincar\'e explicitly sketches but does
not prove; and a modern analytic hypothesis that is needed to make his intended inference rigorous.
Calling all three an ``error'' would be misleading.  The third column therefore states the precise
defect, and labels genuine formula errors explicitly.

\begingroup
\small
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.15}
\begin{landscape}
\begin{longtable}{L{0.075\linewidth}L{0.205\linewidth}L{0.285\linewidth}L{0.355\linewidth}}
\toprule
source & printed role & error, omission, or rigor issue & what this project adds and present status\\
\midrule
\endfirsthead
\noalign{\vskip 18pt}
\toprule
source & printed role & error, omission, or rigor issue & what this project adds and present status\\
\midrule
\endhead
\bottomrule
\endfoot

\S\S90--92 & Introduce the perturbing Hamiltonian, osculating variables, and the first
homological equation. & No isolated formula error is asserted here.  The issue is scope: the
current theorem models the circular restricted principal term, not every coordinate and uniform
first-integral hypothesis of the full three-body discussion. & The repository formalizes the
restricted Hamiltonian derivative, product-rule obstruction, and coordinate-independent algebra.
A faithful full-source theorem would still need the exact Chapter V--VI coordinate maps and
uniform-integral definition.\\

\S93 & Use Darboux's principle to read coefficient asymptotics from boundary logarithms. & The
printed application suppresses the uniform analytic estimates needed when amplitudes and
parameters vary.  A local logarithm alone does not imply that the remaining factor is analytic on
a larger common disk. & Formal coefficient extraction, equal-modulus separation, finite-jet and
Tannery variants, and the larger-disk regular-factor transfer are proved.  Their source-specific
uniform hypotheses remain to be derived from the actual contour integral.\\

\S94 & Form the one-variable Laurent function \(\Phi(z)\) by integrating a Fourier ray around the
unit circle. & Infinite-series convergence, interchange of sum and contour integral, and branch
choices are not fully exposed in the printed argument. & Lean defines the literal normalized
contour integral, proves the finite and absolutely summable affine-ray coefficient extraction, and
constructs the explicit unit-circle path.  The complete physical Fourier series and its common
annulus are still source inputs.\\

\S95 & Locate possible singularities when moving poles obstruct contour deformation. & Solving
the discriminant equations finds candidates but does not show that the original cycle is actually
pinched.  Genuine versus apparent singularities require sheet and cycle data. & The project proves
closed-contour homotopy invariance, winding obstructions, exact cycle splitting, and a local
vanishing-cycle theorem.  Placement of the actual source-sheet cycle into the certified contour
remains global work.\\

\S96, p.~290 & Derive algebraic collision equations using half-angle variables. & \textbf{Printed
formula error:} equation (10) contains \(x^2-1\), while the preceding equation and the stated
reciprocity require \(x^2+1\). & The corrected reciprocal equation is derived and formalized; the
resulting collision cubics, source radicand, concrete candidate \(D\), and fiber double-zero facts
are checked exactly.\\

\S\S97--98 & Decide which candidates are admissible and continue the integration cycle on the
Riemann surface. & Poincar\'e says at the end of \S98 that the discussion is only sketched and that
a complete study of the branches of \(\Phi\) is needed.  The printed text does not construct the
global sheet-preserving deformation. & Lean proves the concrete root branches, synchronized real
parameter, and proves that Poincar\'e's exact $u\mapsto t$ map carries the $u$-unit circle to the
literal \S94 $t$-unit circle through an explicit homotopy of parametrizations at a fixed
parameter.  It also proves that this fixed circle cannot be the continued cycle near $D$: the
pole that begins outside has crossed strictly inside, and winding-number invariance forbids a
pole-avoiding fixed-circle continuation.  Lean further proves
the outer arcs, affine connectors, and the complete seam tables.  The old bulk rows are
now used uniformly in the critical-value coordinate, the homogeneous theorem covers every
positive-fiber collar, and covering-space lifting constructs both connector sheets on
$[0,1)\times[0,1]$.  Lean also proves that each transformed connector integral is literally the
corresponding source curve integral.  The terminal construction now concatenates the two outer
quarters, both connectors, and the inverse-Morse middle path into one closed moving source cycle.
The literal radicand is nonzero on the positive half-open family, one basepoint-normalized lift is
constructed, and its five restrictions are proved equal to the certified branches.  What remains
is no longer the whole preceding transport: 1,408 additional polar cells, checked in 44
LeanCompCert shards, lift the ordinary angular root-coordinate circle through $s=2717/2744$.  The rational
quarter parametrization is proved surjective onto each geometric quadrant, so this statement is
about the standard angular path rather than a substitute polygonal or rational cycle.  The exact
$u\mapsto t$ map and its homotopy identify the corresponding geometric source circle, but
nonvanishing along that homotopy remains to be proved.  The other geometric obligation is the
final $27/2744$ and the deformation from that circle to the near-$D$ five-piece family.\\

\S99 & Move the double zero to a critical center and reduce the radicand to a quadratic pinch. &
Formal Weierstrass manipulation by itself would not establish convergence, a jointly analytic
coordinate change, its Jacobian, or a nonzero pulled-back numerator. & Complex implicit-function,
Hadamard-division, analytic Morse-coordinate, Jacobian one-form, transversality, literal numerator,
and compact local vanishing-cycle theorems are proved for \(D\).\\

\S99, p.~323 & State the leading pulled-back amplitude \(\theta_{0,0}\). & \textbf{Printed formula
error:} after \(\Delta=\tfrac12\Delta_{tt}(t-t_0)^2+\cdots\), the printed factor multiplies by
\(\tfrac12\Delta_{tt}\); the preceding defining identity instead gives a factor proportional to
\(\sqrt{2/\Delta_{tt}}\), up to sheet sign. & The project derives the amplitude from the actual
inverse Morse coordinate and Jacobian.  Its exact value has the reciprocal-square-root behavior
required by the defining identity, rather than assuming the inconsistent printed expression.\\

\S100 & Integrate the prepared inverse square root to obtain
\(\Phi_2+\Phi_3\log(z-z_0)\), then apply Darboux. & The local primitive is plausible, but the
passage to the global source contour and the larger-common-disk control of varying analytic
amplitudes are omitted.  Nonconstant log amplitudes cannot simply be absorbed into an analytic
remainder. & Lean proves the function-level logarithmic primitive, straight-arc integral, nonzero
local coefficient, three- and five-piece asymptotic interfaces, and the required Darboux transfer
under explicit uniform hypotheses.  The source-cycle deformation and regular remainder are still
needed.\\

\S101 & Give the Pallas numerical example. & No defect relevant to the logical nonintegrability
argument is asserted; this is an application rather than a required proof step. & Not formalized,
and not needed for the present theorem.\\

\S102 & Infer that a uniform integral forces the singular-point motion to depend on too few
parameters. & The reference to Chapter~V no.~85 contains a \textbf{printed normalization error}.
With $D_\lambda=B_\lambda\zeta^\lambda$, equation~(13) gives
$dB_\lambda(X_\Phi)=\lambda B_\lambda S$.  The printed relation
$-\zeta H=S$, or $H=-S/\zeta$, does not make $dD_\lambda(H,X_\Phi)$ vanish;
the required relation is $H=-\zeta S$.  The printed argument also does not supply the full
analytic identification of these coefficients with the contour singularities. & Lean formalizes
Fourier integration on the actual two-dimensional unit additive torus.  From one pointwise
first-order Poisson identity, linearity gives the head and five tail coefficient equations;
the six equations are not separately assumed.  The torus model then uses the standard
angle-derivative multiplier and orbital differentiation-under-the-integral rules, resonance
(12 bis), and independence to derive equation~(13), the corrected cancellation, and the complete
rank chain.  An exact rational instance
$ (\lambda,\zeta,B,S)=(1,2,1,1) $ gives residual $3/2$ for the printed normalization.  With the
correction, a common nonzero characteristic direction gives rank at most five; quotienting the
common scale gives rank at most four; independent recovery of the eccentricities gives rank at
most two; and \S103 contradicts this.  It remains to derive the multiplier, interchange, and
independence fields from the full source Hamiltonian and uniform integral, and identify the six
observables with the continued-contour singularity data, including the common radius and
regular-factor estimates.\\

\S103, p.~331 & Differentiate \(P=\sum U^2\) after defining
\(V=x\,\partial U/\partial x-U\). & \textbf{Printed formula error:} the text gives
\(xP_x=2\sum VU+P\); substitution gives \(xP_x=2\sum VU+2P\).  Since the next step restricts to
\(P=0\), this typo does not change the reduced equation. & Lean verifies both the corrected global
identity and its on-curve specialization.  It also proves the concrete sextic/septic geometry,
24-point locus, local multiplicities, moving analytic branches, differential identity, and final
finite rank contradiction.\\

\end{longtable}
\end{landscape}
\endgroup

The contribution of the present note is concentrated in the \S\S97--100 rows: it closes the
scale-sensitive sign needed to join the global outer sheets to the positive local Morse sheet.
It deliberately does not use that local success to conceal the still-open source-cycle and
\S102 hypotheses.

\section{The certified global prefix}

The long-range certificate uses the elementary quarter-circle parametrization
\[
 Q(t)=\frac{1-t^2+2\ii t}{1+t^2},\qquad 0\leq t\leq1,
\]
and its rotations $Q$, $\ii Q$, $-Q$, and $-\ii Q$.  Lean proves that these four maps cover the
ordinary root-coordinate unit circle: in the first quadrant the inverse parameter is
$t=\impart z/(1+\re z)$, and the other quadrants reduce to this one by exact rotations.  Hence a
certificate evaluated using $Q$ applies pointwise to the standard angular parametrization, with
no unproved reparametrization assumption.

\begin{center}
\begin{tabular}{lll}
\toprule
item & exact value & kernel-checked conclusion\\
\midrule
radial mesh & 28 cubic-clustered cells & first 22 rows used on pinching half\\
angular mesh & 32 quadratic-clustered cells per quarter & continuum cover, not point samples\\
new artifacts & $2\cdot22=44$ & independent verified-program verdicts\\
new polar cells & $2\cdot22\cdot32=1408$ & strictly positive real radicand\\
terminal prefix & $1-(6/28)^3=2717/2744$ & one principal sheet on the full root circle\\
uncertified tail & $1-2717/2744=27/2744$ & must meet the local five-piece family\\
\bottomrule
\end{tabular}
\end{center}

The fixed cutoff cannot by itself prove overlap with the analytic Morse neighborhood: the local
inverse-function argument supplies a positive neighborhood but no numerical lower bound for its
size.  Closing the tail therefore needs either a quantitative inverse/Morse estimate, an
all-precollision analytic nonvanishing theorem, or a certified deformation whose endpoint is
proved to lie inside the selected local neighborhood.  Merely increasing the finite table to a
closer fixed rational number would not logically remove this issue.

\section{The centered collision factor}

Let \(x_D\) be the unique root in \([-0.027,-0.026]\) of
\[
  2500x^3+500025x^2+12501x-25=0,
\]
let \(D<0\) be its real cube root, and put
\[
  y_D=\frac{(x_D-1/100)^2}{2(1+(1/100)^2)x_D}.
\]
Write \(F(\zeta,u)\) for the collision factor which vanishes twice at
\((\zeta_D,D)\), and write \(G=\partial F/\partial u\). Direct algebra, with every
inverse-power difference cancelled before evaluation, gives
\begin{equation}\label{eq:centered-expansion}
 G(\zeta,u)
 =A(u-D)+(u-D)^2K(u)
   +\left(\frac{\zeta}{\zeta_D}-1\right)P(u),
\end{equation}
where
\[
 A=\frac{\partial^2F}{\partial u^2}(\zeta_D,D)\in\R,
 \qquad A<0.
\]
The explicit formulas used by the calculation are in Appendix~\ref{sec:formulas}.

Set
\[
 E(a)=
 \begin{cases}
  0,&a=0,\\
  \dfrac{e^a-1-a}{a^2},&a\ne0.
 \end{cases}
\]
Then
\begin{equation}\label{eq:exp-remainder}
 e^a=1+a+a^2E(a),
 \qquad |a|\leq1\Longrightarrow |E(a)|\leq1.
\end{equation}
The nonstandard value at \(a=0\) is harmless because it is multiplied by \(a^2\).

\section{Scale-free connector coordinates}\label{sec:boxes}

Let \(s=-1\) on the initial connector and \(s=+1\) on the final connector. Let \(L>0\)
be the selected Morse half-length and let \(\delta\) be distance from the local endpoint:
\[
 \delta=1-t\quad(s=-1),\qquad \delta=t\quad(s=+1).
\]
On both collars, \(0\leq\delta\leq261/1024\). Normalize the moving inputs by
\[
 u_{\mathrm{loc},s}-D=sLq_s,\qquad
 u_{\mathrm{out},s}(\kappa)-u_{\mathrm{out},s}(0)=L^2r_s,\qquad
 \frac{\zeta}{\zeta_D}-1=L^2p.
\]
The inverse-Morse model can be shrunk so that
\begin{equation}\label{eq:primitive-bounds}
 0<L\leq2^{-20},\qquad
 |\re r_s|,|\impart r_s|,|\re p|,|\impart p|\leq1,
\end{equation}
and
\begin{equation}\label{eq:q-box}
 -\frac{1024}{2^{20}}\leq\re q_s\leq\frac{1024}{2^{20}},
 \qquad
 -\frac{158311}{2^{20}}\leq\impart q_s\leq-\frac{130048}{2^{20}}.
\end{equation}
This last box is the quantitative form of the downward inverse-Morse tangent.

At collapsed scale the local-to-outer directions are
\[
 B_-=-D(1+\ii),\qquad B_+=-D(1-\ii).
\]
Define the actual direction and coordinate by
\begin{equation}\label{eq:direction-coordinate}
 V_s=B_s+L^2r_s-sLq_s,\qquad
 u-D=sLq_s+\delta V_s.
\end{equation}
These are identities. In particular, all occurrences of the small endpoint displacement
share the same \(L\).

\section{The homogeneous real-part identity}

Substitute \eqref{eq:direction-coordinate} and the normalized parameter displacement into
\eqref{eq:centered-expansion}. Define
\begin{align}
 X_s={}&Asq_sV_s
 +\delta A(Lr_s-sq_s)(V_s+B_s) \notag\\
 &+\bigl(Lq_s^2+2s\delta q_sV_s\bigr)K(u)V_s
 +LpP(u)V_s,\label{eq:X}\\[3pt]
 Z_s={}&V_s^3K(u).\label{eq:Z}
\end{align}

\begin{lemma}[Exact cancellation]\label{lem:cancellation}
For either connector,
\begin{equation}\label{eq:homogeneous-identity}
 \re\bigl(G(\zeta,u)V_s\bigr)=L\re X_s+\delta^2\re Z_s.
\end{equation}
\end{lemma}

\begin{proof}
Expand the left side using \eqref{eq:centered-expansion} and
\(u-D=sLq_s+\delta V_s\). Also write
\(V_s=B_s+L(Lr_s-sq_s)\). Collect every term containing \(L\) into
\(LX_s\), and collect the quadratic-distance term into \(\delta^2Z_s\).
The only apparent term left over is \(\delta AB_s^2\). Since \(A\) is real and
\[
 B_-^2=2\ii D^2,\qquad B_+^2=-2\ii D^2,
\]
its real part is exactly zero.
\end{proof}

Neither coefficient in \eqref{eq:homogeneous-identity} is divided by \(L\). The first term
retains a positive factor \(L\), while the collapsed part begins at order \(\delta^2\).
Thus the coefficient boxes can remain separated from zero as \(L\to0^+\).

\section{Finite interval lemma}

The primitive dyadic boxes, all at denominator \(2^{20}\), are
\begin{align*}
 D&\in[-314053,-314047]2^{-20},&
 y_D&\in[-28312,-25000]2^{-20},\\
 \frac{100}{30003}&\in[3494,3495]2^{-20},&
 \frac{200}{10001}&\in[20969,20970]2^{-20},\\
 \frac1{10001}&\in[104,105]2^{-20}.&&
\end{align*}
The cubic is strictly decreasing on
\([-0.026865395705,-0.026865395704]\), takes opposite signs at its endpoints,
and its negative real cube root lies in the displayed \(D\)-box. Substitution in the
formula for \(y_D\) gives the displayed \(y_D\)-box.

Cover the distance interval by the 160 uniform cells
\[
 I_j=[1671j/2^{20},1671(j+1)/2^{20}],\qquad 0\leq j<160.
\]
Indeed, \(160\cdot1671/2^{20}=0.254974365234375>261/1024\).
On each cell and side, rectangular complex interval arithmetic evaluates
\eqref{eq:direction-coordinate}, the Appendix formulas, and \eqref{eq:X}--\eqref{eq:Z}.
The exponential remainder \(E(a)\) is enclosed by
\([-1,1]+\ii[-1,1]\), using \eqref{eq:exp-remainder}; the same interval evaluation first
checks \(|a|\leq0.218249762481<1\) on every cell. All other normalized moving
inputs use \eqref{eq:primitive-bounds}--\eqref{eq:q-box} independently, deliberately
overestimating their joint range.

\begin{proposition}[Interval result]\label{prop:interval}
For \(s=-1,+1\), every admissible normalized input, and
\(0\leq\delta\leq261/1024\),
\[
 \re X_s\geq0.0041922727090,\qquad
 \re Z_s\geq0.0655826074416.
\]
\end{proposition}

The independently computed enclosures are:
\begin{center}
\begin{tabular}{lcc}
\toprule
 & initial connector & final connector\\
\midrule
minimum lower endpoint of \(\re X_s\) & 0.00419227270907 & 0.00419227270907\\
maximum upper endpoint of \(\re X_s\) & 0.218433271265 & 0.218433271265\\
minimum lower endpoint of \(\re Z_s\) & 0.0655826074417 & 0.0655826074417\\
maximum upper endpoint of \(\re Z_s\) & 0.776610992074 & 0.776610992074\\
\bottomrule
\end{tabular}
\end{center}
The worst lower bound for \(X_s\) occurs in cell \(159\), at the outer collar boundary;
the worst lower bound for \(Z_s\) occurs in cell \(0\), at the local endpoint.
The two row lists agree exactly after conjugating the imaginary coordinate, so the checked
one-side table in \path{research/chapter_vi_connector_seam_table.csv} represents both
connectors. The verifier recomputes this symmetry rather than assuming it.

The calculation is reproduced by
\begin{verbatim}
python3 research/check_chapter_vi_connector_seam.py
\end{verbatim}
The script uses only the Python standard library. Every elementary binary64 result is widened
by one representable number toward each infinity. This is strong independent evidence and is
close to a conventional interval proof, but it is not a kernel-checked certificate.

\section{LeanCompCert formalization}

The same calculation is now encoded independently in
\path{ChapterVIDHomogeneousCompiledTable.lean}.  Its primitive intervals and all intermediate
rectangles have denominator $2^{20}$.  A row is not merely a list of final decimal endpoints: it
is a straight-line trace containing the checked additions, multiplications, reciprocals, powers,
and the rigorous exponential-remainder enclosure that produce those endpoints.  Lean proves by
structural interval lemmas that soundness of these operations implies containment of the literal
complex expressions in \eqref{eq:X}--\eqref{eq:Z}.

There are 160 distance cells on each side.  Ten rows, together with a 39-operation fixed prelude,
form one artifact with 2609 dyadic operations and 23197 signed-integer claims.  Thus the complete
table consists of 32 independently checkable artifacts and 320 semantic rows.  Generated Lean
theorems, evaluated by the kernel rather than a native-decision shortcut, prove that every
artifact lies inside LeanCompCert's signed 64-bit fragment and that every one of its 23197
signed claims holds.  LeanCompCert's verified denotation theorem therefore proves that every
reference artifact returns zero.  The independent reference evaluation gives

\begin{center}
\begin{tabular}{lr}
\toprule
quantity & worst precision-20 numerator\\
\midrule
lower bound for $\re X_s$ & $3638$\\
lower bound for $\re Z_s$ & $68596$\\
upper bound for the exponential argument's $\ell^1$ norm & $250724$\\
failed signed-integer claims & $0$\\
\bottomrule
\end{tabular}
\end{center}
Hence the formal table proves the slightly more conservative but still strict bounds

\[
  \re X_s\geq\frac{3638}{2^{20}}>0,
  \qquad
  \re Z_s\geq\frac{68596}{2^{20}}>0,
  \qquad
  \lVert a\rVert_1\leq\frac{250724}{2^{20}}<1.
\]

The reference verdict is thus unconditional: it is derived from kernel proofs of the claims and
the verified program semantics, not from an empirical execution premise.  From that verdict,
ordinary Lean theorems prove the two coefficient inequalities, the oriented derivative signs,
the two crossing certificates, and finally the seam-compatible five-piece logarithmic limit.
LeanCompCert can also derive emitted C from the exact operation lists; a hash-bound receipt
interface is retained for optional independently compiled reruns.  Neither such a receipt nor the
Python table is used by the reference Lean theorem.

The reference computation is reproduced by
\begin{verbatim}
lake exe chapter-vi-connector-cert reference-homogeneous-shards
\end{verbatim}
and reports 32 checked shards, 320 checked cells, and zero failed claims.

\section{Conclusion for the seam}

\begin{theorem}[Oriented derivative on both collars]\label{thm:orientation}
For a sufficiently small anchored Morse model satisfying
\eqref{eq:primitive-bounds}--\eqref{eq:q-box},
\[
 -\frac{d}{dt}\re F(\zeta,u_-(t))>0
 \quad\text{on the initial collar},
\]
and
\[
 \frac{d}{dt}\re F(\zeta,u_+(t))>0
 \quad\text{on the final collar}.
\]
\end{theorem}

\begin{proof}
The oriented derivative is the left side of \eqref{eq:homogeneous-identity}.
The formal LeanCompCert bounds, \(L>0\), and \(\delta^2\geq0\) give
\[
 L\re X_s+\delta^2\re Z_s
 \geq\frac{3638}{2^{20}}L+\frac{68596}{2^{20}}\delta^2>0.
\]
Proposition~\ref{prop:interval} gives the stronger independent Python bounds quoted above.
The orientation reverses the initial connector and leaves the final connector unchanged.
\end{proof}

At the local endpoint, the real part of the first factor is already strictly positive:
the product radicand is positive real there and the companion factor has positive real part.
On the initial connector, Theorem~\ref{thm:orientation} says that the first factor's real part
is nonincreasing as \(t\) approaches \(1\), so every earlier collar value is at least its
positive endpoint value. On the final connector it is nondecreasing away from \(t=0\).
Consequently
\[
 \re F(\zeta,u_s(t))>0
 \quad\text{throughout both endpoint collars}.
\]

In particular, if the imaginary part of the first factor vanishes at its unique possible
crossing, its real part is positive. The principal square root therefore extends from the
outer boundary to the prescribed local Morse root without crossing its branch cut. The
existing positive-real result for the companion factor supplies the other square root.
Their product gives the compatible radicand sheet on each connector, and the two local seam
signs are the required positive Morse signs. Together with the already constructed outer arcs
and pinched middle arc, this supplies the seam-compatible five-piece logarithmic contribution.

\section{What the certificate actually computes}\label{sec:certificate-computation}

The certificate is needed because the endpoint displacement tends to zero with the selected
Morse length.  A direct rectangular enclosure of the unscaled derivative forgets that several
small quantities are the same quantity.  It consequently replaces exact cancellation by an
interval containing both signs.  The certificate does not establish a new abstract continuity
principle.  It verifies the finite uniform inequalities that remain after the analytic proof has
retained the common scale symbolically.

For each side and distance cell, the Lean definition constructs the following dependency graph:
\[
\begin{array}{c}
(D,y_D,c,b,10001^{-1}),\quad L,\delta,q_s,r_s,p\\[2pt]
\downarrow\\[-2pt]
L^2r_s,\ Lq_s,\ V_s=B_s+L^2r_s-sLq_s,\ u=D+sLq_s+\delta V_s\\[2pt]
\downarrow\\[-2pt]
u^{-1},u^{-2},u^{-3},u^{-4},\ H(u),M(u),a=(u-D)H(u)\\[2pt]
\downarrow\\[-2pt]
E(a),\ P(u),\ K(u)\\[2pt]
\downarrow\\[-2pt]
X_s,\ Z_s,\ \re X_s>0,\ \re Z_s>0.
\end{array}
\]
Every arrow is represented by primitive dyadic interval operations.  Products check all four
endpoint products with outward rounding.  Positive reciprocals first certify that the input
interval is strictly positive and then certify both reciprocal endpoints.  Complex products are
four real products followed by exact signed additions.  Powers and the removable inverse-power
differences in Appendix~\ref{sec:formulas} are built from these primitives; no floating-point
transcendental function occurs in the artifact.

The relative exponential is treated analytically.  Lean proves
\[
 e^a=1+a+a^2E(a),\qquad \lVert a\rVert\leq1\Longrightarrow \lVert E(a)\rVert\leq1,
\]
and the table checks the stronger raw bound
\(\lVert a\rVert_1\leq250724/2^{20}<1\).  It then encloses the unknown complex value
\(E(a)\) by the unit square.  Thus the finite computation only performs rational interval
arithmetic; the transcendental estimate is an ordinary analytic theorem.

The fixed prelude contains 39 operations for the collision constants and base coefficients.
Each cell contributes 257 further operations.  One ten-cell shard therefore has
\[
 39+10\cdot257=2609
\]
operations.  Flattening their interval obligations produces 23197 signed product comparisons.
There are 16 shards per side, so the full formal cover contains 32 artifacts and 320 cells.
The fixed prelude is repeated in each shard to make every artifact independently checkable.

\section{From integer comparisons to the analytic theorem}

The formal proof has five distinct layers.

\begin{enumerate}
\item \emph{Primitive containment.}  Lean proves that the actual constants and normalized moving
inputs lie in the declared dyadic boxes.  In particular the inverse-Morse derivative estimate and
the selector supply the box for \(q_s\), rather than treating it as numerical input.

\item \emph{Word-size admissibility.}  For each shard the generated theorem
\path{shard_admissible_SIDE_INDEX} proves in the kernel that every signed intermediate used
by LeanCompCert fits the verified 64-bit fragment.

\item \emph{Arithmetic truth.}  The companion theorem
\path{shard_allHold_SIDE_INDEX} proves all 23197 signed comparisons by kernel reduction.
LeanCompCert's verified program-denotation theorem converts admissibility plus these comparisons
into the unconditional theorem that the corresponding checker program returns zero.

\item \emph{Interval semantics.}  The generic multiplication, reciprocal, and complex-trace
lemmas turn the zero verdict into containment of every literal real or complex expression.
The theorem \path{CellTrace.coefficients_pos_of_allSound} extracts the strict signs of
\(\re X_s\) and \(\re Z_s\) for an arbitrary point covered by a row.

\item \emph{Calculus and sheet assembly.}  Lean identifies the row's coordinate with the literal
affine connector coordinate, invokes the exact identity
\eqref{eq:homogeneous-identity}, and constructs an
oriented-real derivative certificate on each collar.  Existing one-variable calculus turns
these into the two positive crossing certificates.  The already certified companion factor,
outer arcs, and connector regularity then construct compatible square-root sheets and the
five-piece logarithmic limit.
\end{enumerate}

The exported reference-model existence theorem has no numerical run,
receipt, floating-point, or CSV premise.  Its axiom audit contains only the standard mathlib
foundational axioms \texttt{propext}, \texttt{Classical.choice}, and \texttt{Quot.sound}.

The same construction has now been applied to every older finite table consumed by the seam
assembly: the 56 outer-polar shards, 64 connector-factor bulk shards, two endpoint anchors,
41 first-derivative shards, and 10 second-derivative shards.  For each shard Lean proves both
word-size admissibility and all signed comparisons, then obtains the zero-return theorem from
LeanCompCert's verified semantics.  The reference run verdicts therefore have no external
execution premise.  The newer theorem
\path{radicand_ne_zero_on_puncturedRectangle} combines those verdicts with the homogeneous
collar argument and discharges nonvanishing at every positive critical value.  Its square-root
sheet lives on the natural half-open parameter rectangle.  The near-$D$ gluing to the collision
sheet and both outer sheets is now formalized; joining that terminal family to the original
source cycle is analytic topology, not a missing execution receipt.

One further bookkeeping gap has been removed in Lean:
\path{connectorIntegral_eq_principal_curveIntegral} constructs the actual source-coordinate
connector path and proves the exact pullback identity, including both the affine velocity and
Poincar\'e's nonlinear \(u\mapsto t\) Jacobian.  The former second interface,
\path{principalPhiAlongCriticalValue}, has deliberately been removed.  It re-evaluated the
literal unit circle at the current parameter and therefore named the wrong analytic-continuation
object.  Theorems in \path{ChapterVIDRadialContour.lean} prove that the outside collision pole
eventually lies inside that circle, and \path{ChapterVIWindingObstruction.lean} proves that no
pole-avoiding continuation can preserve the incompatible winding numbers.  The missing object is
a lift of the original germ through the long parameter range before the certified near-$D$
jointly moving cycle begins.

For \S102, the primary text on pp.~326--329 gives a staged parameter count rather than assuming a
two-coordinate factorization at the outset.  The new theorem
\path{collisionOrientation_rank_le_two_of_ratio_rank_le_four} formalizes that count for an
arbitrary family of collision roots.  The theorem
\path{not_fourParameterRatioFactorization} then connects the resulting rank bound directly to the
24 certified moving branches of \S103.  The same file now formalizes the preceding Chapter~V
calculation rather than assuming (13 bis): it derives equation~(13) from the first-order Fourier
coefficient identity and resonance, proves the corrected $H=-\zeta S$ cancellation, proves the
characteristic direction nonzero from independence, and obtains rank five by rank--nullity.  The
exact theorem \path{chapterVNo85_printed_normalization_counterexample} records why the printed
$-\zeta H=S$ cannot be used literally.

\section{The terminal lifted contour and Chapter~V extraction}

The finite tables now feed an actual two-dimensional continuation object rather than five
unrelated paths.  Let
\[
  P=[0,1)\times[0,1],
\]
where the first coordinate is the positive critical-value parameter and the second traverses the
closed contour.  The function \path{positiveContourPoint} concatenates, in order, the initial
outer quarter, upper connector, inverse-Morse middle segment, lower connector, and final outer
quarter.  Nested path concatenation is reparametrized exactly by
\[
 [0,\tfrac14],\quad [\tfrac14,\tfrac12],\quad
 [\tfrac12,\tfrac58],\quad [\tfrac58,\tfrac34],\quad
 [\tfrac34,1].
\]
The endpoint equalities are proved in the path types, so this is a closed path for every parameter
and not an algebraic formal sum of integrals.

Write $R(s,t)$ for Poincar\'e's literal transformed source radicand evaluated on this path.
Lean proves
\[
  R(s,t)\ne0\qquad ((s,t)\in P)
\]
by dispatching each of the five subintervals to the compiled outer tables, the full connector
tables, or the exact identity $R=k+v^2$ on the positive Morse segment.  Continuity and the
covering-space lift for $w\mapsto w^2$ produce a continuous $W:P\to\mathbb C$ with
\[
  W(s,t)^2=R(s,t).
\]
It is normalized to the principal root at one basepoint.  Connectedness and square-root
uniqueness then propagate that normalization across every seam.  The theorem
\path{exists_jointSheet_eq_allCertifiedPieces} states simultaneously that $W$ restricts to the
two compiled principal outer roots, both seam-compatible connector roots, and the positive Morse
root.  Thus the entire terminal five-piece branch choice is kernel checked.  This theorem starts
inside the certified near-$D$ neighborhood; it does not invent the unprinted long-range homotopy
from the original \S94 circle.

The Chapter~V layer is likewise now an analytic extraction rather than six unrelated scalar
assumptions.  On Mathlib's two-dimensional unit additive torus, suppose one has the pointwise
first-order identity
\[
 -S\,\partial_r B+T\,\partial_r C+d_XB=0.
\]
Linearity of Haar integration proves that its Fourier coefficient at a selected mode \(\lambda\)
satisfies
\[
 -S\,\lambda B_\lambda+T\,\lambda C_\lambda+dB_\lambda(X)=0,
\]
once the standard Fourier multiplier and differentiation-under-the-integral identities are
supplied.  Applying this one theorem to a head mode and five tail modes constructs all six fields
of the formal resonant first-order data structure.  Resonance $T=0$, independence, and the corrected
normalization $H=-\zeta S$ then give the common characteristic direction and the rank chain
\[
  6\longrightarrow5\longrightarrow4\longrightarrow2,
\]
which the certified \S103 calculation contradicts.  What is still source-specific is deriving
the multiplier, interchange, resonance, and independence hypotheses from the full Hamiltonian
and putative uniform integral, and proving that these six Fourier observables are exactly the
common-radius contour-singularity observables.

\section{Independent checks and reproducibility}

The kernel proof and the compact Python computation intentionally use different implementations.
They agree on the sign and have comfortably positive margins; the Python implementation obtains
the stronger enclosures in Proposition~\ref{prop:interval}, while the formal table obtains the
more conservative dyadic bounds used in Theorem~\ref{thm:orientation}.  The following commands
reproduce the checks from the repository root:

\begin{verbatim}
python3 research/check_chapter_vi_connector_seam.py
lake build PoincareChapterVI.ChapterVIDHomogeneousAdmissibility
lake exe chapter-vi-connector-cert reference-homogeneous-shards
lake env lean PoincareChapterVI/AxiomAudit.lean
lake build
\end{verbatim}

The observed reference summary is
\begin{verbatim}
checked homogeneous shards: 32
checked homogeneous cells: 320
failed integer claims: 0
minimum endpoint-coefficient lower numerator / 2^20: 3638
minimum distance-coefficient lower numerator / 2^20: 68596
maximum exponential-argument L1 numerator / 2^20: 250724
\end{verbatim}

The executable can also emit any shard as straight-line C and submit it to CompCert:
\begin{verbatim}
lake exe chapter-vi-connector-cert \
  emit-homogeneous-shard initial 0 shard.c
lake exe chapter-vi-connector-cert \
  check-homogeneous-shard initial 0
\end{verbatim}
The second command requires the CompCert driver \texttt{ccomp} on \texttt{PATH}.  It is an
optional independently compiled rerun; it is not required for the unconditional kernel theorem.

\section{Precise project status}

This work closes the local connector-seam obstruction at the certified point \(D\).  It also
shows that the two obligations previously described as literal source transcription cannot both
be proved as stated: the fixed-current-unit-circle continuation is topologically impossible, and
the normalization printed in Chapter~V no.~85 is algebraically false.  The corrected finite
no.~85 calculation is now formalized.  Poincar\'e's complete Chapter~VI proof is not thereby
complete.

Two corrected source-reconstruction obligations remain outside the scope of the finite
certificate:
\begin{enumerate}
\item In \S\S95--100, one must lift the original \S94 germ along a \emph{jointly moving} closed
source cycle that avoids every moving radicand zero until it enters the certified near-$D$
family, and control the complementary regular arcs along that transport.  It is not
enough to deform the unit circle re-evaluated at the terminal parameter: Lean proves that the
relevant pole has changed sides of that fixed circle.  The terminal five-piece contour, its
positive-parameter square-root sheet, and the identification of all five branches are closed;
what is missing is the earlier global joint cycle and sheet transport.

\item In \S102, one must derive the recorded Fourier multiplier, orbital-interchange, resonance,
and independence fields from the full source Hamiltonian and Chapter~V uniform integral, and
identify the six rescaled coefficients with the common-radius singularity data of the continued
contour.  Once supplied, the corrected (13 bis) construction, five-to-four-to-two rank chain,
Darboux transfer, and \S103 contradiction apply without another finite-algebra premise.
\end{enumerate}

These are analytic source-reconstruction problems, not further rows missing from the interval
table.  Enlarging the present certificate would not discharge them.

\section{Formal source map}

The main files supporting this note are:
\begin{center}
\begin{tabular}{L{0.43\textwidth}L{0.48\textwidth}}
\toprule
file & role\\
\midrule
\path{ChapterVIDHomogeneousDerivative.lean} & exact homogeneous cancellation and semantic
coefficient certificate\\
\path{ChapterVIDHomogeneousCompiledTable.lean} & dyadic traces, cell cover, semantic soundness,
and seam theorem bridge\\
\path{ChapterVIDHomogeneousAdmissibility.lean} & aggregate unconditional reference verdict and
exported model-existence theorem\\
\path{ChapterVIDConnectorFullBulk.lean} & uniform two-dimensional reuse of the bulk table,
positive-fiber collar closure, and continuous square-root sheets on the two half-open connector
rectangles\\
\path{ChapterVIDJointLiftedContour.lean} & one closed terminal five-piece source contour,
literal-radicand nonvanishing on its positive half-open parameter family, a basepoint-normalized
joint square-root sheet, and exact identification with all five certified branches\\
\path{ChapterVIWindingObstruction.lean}, \path{ChapterVIDRadialContour.lean} & winding invariance,
the concrete outside-to-inside crossing, and impossibility of the former fixed-unit-circle
continuation premise\\
\path{ChapterVISection102.lean} & pre-resonance no.~85 Fourier equation, exact counterexample to
the printed normalization, corrected equation-(13 bis) kernel construction, and complete rank
chain from six scaled values to the \S103 contradiction\\
\path{ChapterVNo85FourierExtraction.lean} & actual two-torus Haar Fourier extraction of the head
and five tail equations from one pointwise first-order Poisson identity\\
\path{HomogeneousAdmissibility/*.lean} & independently buildable kernel proofs for all 32
shards\\
\path{ConnectorCertificateMain.lean} & reference, statistics, emission, and native-check
commands\\
\path{check_chapter_vi_connector_seam.py} & independent outward-rounded Python audit\\
\path{chapter_vi_connector_seam_table.csv} & compact symmetric Python table\\
\path{generate_chapter_vi_homogeneous_admissibility.py} & reproducible generator for the
kernel shard modules\\
\bottomrule
\end{tabular}
\end{center}

\appendix
\section{Explicit coefficient formulas}\label{sec:formulas}

Put
\[
 c=\frac{100}{30003},\qquad b=\frac{200}{10001},\qquad
 Q=u^2+uD+D^2,\qquad Q_1=u+2D.
\]
The coefficient of \(u-D\) in the relative exponential argument and the multiplier are
\begin{align*}
 H(u)&=-cQ\bigl(1+u^{-3}D^{-3}\bigr),\\
 M(u)&=-2y_DD^{-1}+by_DD^{-1}(u^{-3}+u^3).
\end{align*}
The argument in \eqref{eq:exp-remainder} is \(a=(u-D)H(u)\), and
\[
 P(u)=\bigl(1+a+a^2E(a)\bigr)M(u).
\]

For the quadratic coefficient, introduce
\begin{align*}
 \eta_2&=-\frac{u+D}{u^2D^2},&
 \eta_4&=-\frac{u^3+u^2D+uD^2+D^3}{u^4D^4},\\
 T&=D^{-4}(u^{-2}+D^2u^{-4}),&
 T_1&=D^{-4}(\eta_2+D^2\eta_4),\\
 R_L(u)&=\frac1{10001}\bigl((30000+3T)+6DT_1\bigr),\\
 \eta_3&=-\frac{Q}{u^3D^3},\\
 R_S(u)&=D^{-1}\left[Q_1(1-u^{-3}D^{-3})
      +3D^2(-D^{-3}\eta_3)\right],\\
 H_1(u)&=-c\left[Q_1(1+u^{-3}D^{-3})+3D^2(D^{-3}\eta_3)\right],\\
 M_1(u)&=by_DD^{-1}(\eta_3+Q).
\end{align*}
Then
\[
 K(u)=R_L(u)+by_DR_S(u)+H_1(u)M(u)+H(D)M_1(u)+H(u)^2E(a)M(u).
\]

Finally, define
\begin{align*}
 C_L(u)&=\frac{u+D}{10001}
 \left(30000+3\frac{u^2+D^2}{u^4D^4}\right),\\
 C_S(u)&=QD^{-1}(1-u^{-3}D^{-3}),\\
 C(u)&=C_L(u)+by_DC_S(u).
\end{align*}
The base coefficient is evaluated without numerical differentiation:
\[
 A=C(D)+H(D)M(D),\qquad
 -2.69848482771\leq A\leq-2.56120107040.
\]

\end{document}
